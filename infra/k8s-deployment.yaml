apiVersion: apps/v1
kind: Deployment
metadata:
  name: td-migrationbridge-fe-angularjs
  labels:
    app: td-migrationbridge-fe-angularjs
spec:
  replicas: 2
  selector:
    matchLabels:
      app: td-migrationbridge-fe-angularjs
  template:
    metadata:
      labels:
        app: td-migrationbridge-fe-angularjs
    spec:
      containers:
        - name: app
          image: td-migrationbridge-fe-angularjs:18
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: td-migrationbridge-fe-angularjs
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: td-migrationbridge-fe-angularjs
